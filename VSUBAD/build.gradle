buildscript {
  repositories {
    jcenter()
    google()
  }
  dependencies {
    classpath 'com.android.tools.build:gradle:3.1.2'
  }
}

allprojects {
  repositories {VSU-Online
  }
}

task clean(type: Delete) {
  delete rootProject.buildDir
}

